//declaring two variables each for a separate function (delete1 in JS = deletButton in HTML)
var deleteEvent = document.getElementById("deleteButton");
delete1.addEventListener('click', myDelete,false);

//declaring two variables each for a separate function (add1 in JS = addButton in HTML)
var addEvent = document.getElementById("addButton");
add1.addEventListener('click', myAdd,false);

var people = []

people = ['ronny', 'bobby', 'ricky', 'mike']

document.getElementById("demo").innerHTML = people;

function myDelete ()
{
    
}